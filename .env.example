# PostgreSQL connection used by the FastAPI service.
DATABASE_URL=postgresql+psycopg://tot:tot_password@localhost:5432/trade_of_titans
# Replace before deployment with a long random value.
SECRET_KEY=replace-with-a-long-random-secret
# Comma-separated browser origins permitted to call the API directly.
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
