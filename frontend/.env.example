# Leave empty for local Vite proxy usage. Set this when the API is hosted separately.
VITE_API_BASE_URL=
# Optional override for the Vite development proxy target.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
